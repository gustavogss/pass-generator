import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputText: {   
    backgroundColor: 'red',
    width: '80%',
    padding: 5,
    borderRadius: 4, 
    color: 'white',
    fontSize: 20,
    fontWeight:'300',
    textAlign:'center',    
  }
});